---
- name: Copy and run script
  hosts: all
  remote_user: azureuser
  become: yes
  tasks:
    # - name: Install Python dependencies for Terraform
    #   apt:
    #     name:
    #       - python3-pip
    #       - python3-dev
    #     state: present

    # - name: Install Terraform
    #   apt:
    #     name: terraform
    #     state: present

    - name: Install Docker
      apt:
        name: docker
        state: present

    - name: Install Docker Compose
      apt:
        name: docker-compose
        state: present

    - name: Install kubectl
      apt:
        name: kubectl
        state: present

    - name: Enable Docker service
      service:
        name: docker
        state: started
        enabled: yes

    - name: Install Terraform
      apt:
        name: terraform
        state: present

    - name: Install Docker
      apt:
        name: docker
        state: present

    - name: Install Docker Compose
      apt:
        name: docker-compose
        state: present

    - name: Install kubectl
      apt:
        name: kubectl
        state: present

    - name: Enable Docker service
      service:
        name: docker
        state: started
        enabled: yes

    